var destino;
destino=prompt("Digite seu destino: ");
switch (destino) {
    case "São Paulo":
        //set de instrução a ser executado, caso a condição seja verdadeira;
        document.write("Seja bem vindo a cidade que nunca para!");
    break;
    case "New York":
        // set de instrução a ser executado, caso a condição seja verdadeira;
        c
    break;
    default:// como se fosse o else
        // set de instruções comandos caso nenhuma das opções anteriores tenha sido escolhida;
        document.write("Não encontramos seu destino!");
}
/*
if(destino=="São Paulo"){
    document.write("Seja bem vindo a cidade que nunca para!");
}
else if (destino=="New York"){
    document.write("Seja bem vindo a cidade que nunca para!");
}
else {
    document.write("Não encontramos seu destino!");
} */